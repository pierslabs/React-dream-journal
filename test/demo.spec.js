const { render, renderHook } = require('@testing-library/react');
const { useForm } = require('../src/hooks/useForm');

describe('Demo', () => {
  test('should first', () => {
    console.log('demo');
  });
});
